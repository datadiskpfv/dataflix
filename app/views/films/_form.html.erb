<%= simple_form_for(@film) do |f| %>

  <%= f.input :title, :input_html => {:style => "width: 30%;"} %>
  <%= f.input :description, :as => :text, :input_html => {:rows => 10, :style => "width: 50%;"} %>

  <%= f.button :submit, class: "btn-primary" %>
<% end %>
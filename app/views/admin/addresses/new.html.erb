<header>
  <h1>New Address</h1>
</header>

<%= render 'form', address: @address %>

<% if @address.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@address.errors.count, 'error') %>
      prohibited this film from being created: </h2>

    <ul>
      <% @address.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<header>
  <h1>Edit Address</h1>
</header>

<%= render 'form', country: @address %>

<% if @address.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@address.errors.count, 'error') %>
      prohibited this film from being created: </h2>

    <ul>
      <% @address.errors.each do |attr, msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>